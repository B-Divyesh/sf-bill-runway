[
  {
    "id": "first-gap",
    "claim": "The timeline identifies the first uncovered bill amount.",
    "where": "planner, README",
    "test": "npx playwright test --grep @claim:first-gap",
    "sandbox": "fresh browser context; enter $100.00 available and a $125.50 bill; assert a $25.50 gap"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "first screen, README",
    "test": "npx playwright test --grep @claim:offline-reload",
    "sandbox": "dedicated fresh browser context on /demo; wait for service worker; set offline; reload"
  },
  {
    "id": "twelve-month-view",
    "claim": "Shows a free 60-day or 12-month view without checkout.",
    "where": "first screen, range control, terms, README",
    "test": "npx playwright test --grep @claim:twelve-month-view",
    "sandbox": "fresh browser context; make billing endpoints return the verifier's 404; select 12 months; assert 365 days and zero billing requests"
  },
  {
    "id": "demo-isolation",
    "claim": "The one-click sample opens four realistic entries in a separate workspace that does not alter the real plan.",
    "where": "demo banner, privacy, README",
    "test": "npx playwright test --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; name a real plan; enter demo; assert four named entries; exit; assert the real name remains and the demo database is deleted"
  },
  {
    "id": "reset-demo",
    "claim": "Reset demo restores the original sample without reading or writing the real plan.",
    "where": "demo banner, README, privacy",
    "test": "npx playwright test --grep @claim:reset-demo",
    "sandbox": "fresh browser context; create a named real plan; enter /demo with IndexedDB open calls recorded; mark Electricity paid; reset; assert the four original entries, $900.00, and Mark paid return while only demo:bill-runway opened"
  },
  {
    "id": "csv-export",
    "claim": "Exports the visible upcoming list as CSV.",
    "where": "planner, README",
    "test": "npx playwright test --grep @claim:csv-export",
    "sandbox": "fresh /demo; capture download; assert header, sample bill, and one row per visible event"
  },
  {
    "id": "json-backup",
    "claim": "Imports and exports plan data as JSON.",
    "where": "planner, README, privacy",
    "test": "npx playwright test --grep @claim:json-backup",
    "sandbox": "fresh browser context; import a valid leap-date backup; assert entry; download JSON backup"
  },
  {
    "id": "local-only",
    "claim": "Plan data stays on this device with no analytics or bank connection.",
    "where": "first screen, privacy, README",
    "test": "npx playwright test --grep @claim:local-only",
    "sandbox": "fresh /demo; record all requests while marking paid and resetting; assert none leave the app origin"
  },
  {
    "id": "recurrence-rules",
    "claim": "Recurring entries use calendar-safe month-end dates.",
    "where": "README, planner",
    "test": "npx vitest run -t @claim:recurrence-rules",
    "sandbox": "unit fixture beginning January 31; assert January through April dates clamp to each month end"
  },
  {
    "id": "recurrence-modes",
    "claim": "Handles monthly, weekly, yearly, and one-time entries.",
    "where": "README, bill and income forms",
    "test": "npx vitest run -t @claim:recurrence-modes",
    "sandbox": "unit fixtures; assert the observable scheduled dates for each advertised repeat mode"
  },
  {
    "id": "paid-status",
    "claim": "Records paid bills and lets you undo that status.",
    "where": "README, upcoming list",
    "test": "npx playwright test --grep @claim:paid-status",
    "sandbox": "fresh browser context; add a bill, mark it paid, reload, then undo and assert the status changes"
  },
  {
    "id": "keyboard-controls",
    "claim": "Planner controls remain usable with a keyboard and show focus.",
    "where": "README",
    "test": "npx playwright test --grep @claim:keyboard-controls",
    "sandbox": "fresh 390px browser; activate range, paid-status, demo-reset, and settings-save controls by keyboard; assert focus survives each rerender; Tab to real import input; assert visible 3px focus ring and target spacing"
  },
  {
    "id": "print-layout",
    "claim": "Formats the 60-day upcoming list on one A4 page for printing.",
    "where": "planner, README",
    "test": "npx playwright test --grep @claim:print-layout",
    "sandbox": "fresh /demo with seven visible sample events; generate a Chromium A4 PDF with backgrounds; assert exactly one PDF page while interactive and marketing regions are hidden"
  }
]
