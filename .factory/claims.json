[
  {
    "id": "first-gap",
    "claim": "The timeline identifies the first uncovered bill amount.",
    "where": "planner, README",
    "test": "npx playwright test --grep @claim:first-gap",
    "sandbox": "fresh browser context; enter $100.00 available and a $125.50 bill; assert a $25.50 gap"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "first screen, README",
    "test": "npx playwright test --grep @claim:offline-reload",
    "sandbox": "dedicated fresh browser context on /demo; wait for service worker; set offline; reload"
  },
  {
    "id": "twelve-month-view",
    "claim": "The full 12-month view is free and does not use checkout.",
    "where": "first screen, range control, terms, README",
    "test": "npx playwright test --grep @claim:twelve-month-view",
    "sandbox": "fresh browser context; make billing endpoints return the verifier's 404; select 12 months; assert 365 days and zero billing requests"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo changes do not alter the real plan.",
    "where": "demo banner, privacy, README",
    "test": "npx playwright test --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; name a real plan; enter demo; exit; assert the real name remains and the demo database is deleted"
  },
  {
    "id": "csv-export",
    "claim": "Exports the visible payment run as CSV.",
    "where": "planner, README",
    "test": "npx playwright test --grep @claim:csv-export",
    "sandbox": "fresh /demo; capture download; assert header, sample bill, and one row per visible event"
  },
  {
    "id": "json-backup",
    "claim": "Imports and exports plan data as JSON.",
    "where": "planner, README, privacy",
    "test": "npx playwright test --grep @claim:json-backup",
    "sandbox": "fresh browser context; import a valid leap-date backup; assert entry; download JSON backup"
  },
  {
    "id": "local-only",
    "claim": "Plan data stays on this device with no analytics or bank connection.",
    "where": "first screen, privacy, README",
    "test": "npx playwright test --grep @claim:local-only",
    "sandbox": "fresh /demo; record all requests while marking paid and resetting; assert none leave the app origin"
  },
  {
    "id": "recurrence-rules",
    "claim": "Recurring entries use calendar-safe month-end dates.",
    "where": "README, planner",
    "test": "npx vitest run -t @claim:recurrence-rules",
    "sandbox": "unit fixture beginning January 31; assert January through April dates clamp to each month end"
  },
  {
    "id": "keyboard-controls",
    "claim": "Planner controls remain usable with a keyboard and show focus.",
    "where": "README",
    "test": "npx playwright test --grep @claim:keyboard-controls",
    "sandbox": "fresh 390px browser; Tab to real import input; assert visible 3px label focus ring and target spacing"
  },
  {
    "id": "print-layout",
    "claim": "Formats the payment run for printing.",
    "where": "planner, README",
    "test": "npx playwright test --grep @claim:print-layout",
    "sandbox": "fresh /demo with print media; assert payment run remains visible while interactive and marketing regions are hidden"
  }
]
